{
  "name": "mtg-scanner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "dev:local": "docker compose up -d && pnpm --parallel --filter @mtg-scanner/backend --filter @mtg-scanner/web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "bcrypt",
      "tesseract.js"
    ]
  }
}
